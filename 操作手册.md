# 操作手册

## 1. 环境准备
- Python 3.11 及以上版本。
- 安装依赖：`pip install fastapi uvicorn moviepy pillow numpy httpx toml opencc generativeai google-generativeai`（或使用 uv/pip-tools 导入 `pyproject.toml` 中的依赖）。
- 首次运行会自动创建 SQLite 数据库（默认路径 `data/projects.db`）与素材目录：
  - `assets/images/projects`
  - `assets/audio/generated`
  - `assets/frontend`
  - `outputs`

## 2. 配置文件
编辑 `config.toml`，至少填写以下内容：
1. `volcengine`：用于生成剧本与分镜提示词的模型接口（API URL、API Key、model）。
2. `aliyun_asr`：用于从旁白音频中提取时间戳（AppKey、Token、region）。
3. `minimax_speech`：MiniMax 语音合成接口（API URL、API Key、模型、默认 voice/audio 设置）。
4. `workflow`：可配置默认分镜数量、全局画面风格、负面提示词、输出目录等。

## 3. 使用方式
### 3.1 CLI
```bash
python main.py --theme "城市微光" \
               --style "温暖治愈" \
               --image-style "电影级写实" \
               --scene-count 8 \
               --auto-confirm
```
- `--theme`（必填）：主题/创意方向。
- 其他可选参数：`--style`、`--image-style`、`--negative-prompt`、`--voice-id`、`--voice-speed`、`--scene-count`、`--aspect-ratio` 等。
- 无 `--auto-confirm` 时，CLI 会输出分镜信息并等待确认；生成图像后执行 `python main.py --project-id <id>` 可继续渲染。

### 3.2 Web 前端
1. 运行 `uvicorn main:app --reload --port 8000`。
2. 浏览器访问 `http://localhost:8000/`。
3. 表单输入主题、语气、分镜数、voice 等参数，点击“生成剧本与提示词”。
4. 下方表格中可编辑每个分镜的脚本、图片提示词、负面提示词和图片路径。
5. 点击“保存镜头修改”存盘；确认无误后点击“确认并合成”触发语音与视频生成。

## 4. 工作流步骤
1. **生成剧本与提示词**：使用 Volcengine 模型得到每个分镜的旁白与图片提示词，结果写入 SQLite。
2. **准备分镜图片**：根据提示词（正向/负向）在本地 ComfyUI 或其他工具生成图片，并保存到 `assets/images/projects/project_<id>/scene_<n>.png`。
3. **人工校验**：确认脚本、提示词、图片路径是否正确，可在 Table/CLI 输出中修改。
4. **确认合成**：系统调用 MiniMax TTS 生成旁白音频（`assets/audio/generated/project_<id>.mp3`），再结合 ASR+AI 计算的时长、图片、背景音乐生成视频（`outputs/project_<id>.mp4`）。

## 5. 常见问题
| 问题 | 处理方式 |
| ---- | -------- |
| `ModuleNotFoundError: fastapi` | 未安装依赖，执行 `pip install fastapi uvicorn`。 |
| `Missing storyboard images` | 未按路径放置 `scene_<n>.png`，根据提示词生成图片并重新放入 `assets/images/projects/project_<id>/`。 |
| API 调用失败/超时 | 检查 `config.toml` 中各服务的 URL、API Key、模型参数是否正确，必要时更换网络环境。 |
| TTS 输出为空 | 确认 MiniMax 接口已开通、`voice_setting`/`audio_setting` 配置符合官方要求。 |

## 6. 后续扩展建议
- 在 SQLite 中保留项目历史版本，支持回溯。
- 增加模板管理（不同主题/风格预设）。
- 引入批量渲染队列与状态监控面板。
