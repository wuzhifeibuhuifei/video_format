# 使用手册

## 1. 环境准备
- Python 3.11 及以上版本。
- 安装依赖：`pip install fastapi uvicorn moviepy pillow numpy httpx toml opencc generativeai google-generativeai`（或使用 uv/pip-tools/poetry 安装 `pyproject.toml` 中的依赖）。
- 首次运行会自动创建 SQLite 数据库（默认 `data/projects.db`）及素材目录：`assets/images/projects`、`assets/audio/generated`、`assets/frontend`、`outputs`。

## 2. 配置文件（`config.toml`）
1. `volcengine`：剧本/分镜提示词模型（API URL、API Key、model）。
2. `aliyun_asr`：阿里云极速版 ASR（AppKey、Token、region）。
3. `minimax_speech`：MiniMax 语音合成（API URL、API Key、model 及默认 voice/audio 设置）。
4. `workflow`：默认分镜数量、全局风格、负面提示词、输出目录等。
5. `comfyui`：本地 ComfyUI 配置示例：
   ```toml
   [comfyui]
   enable_auto_generate = true          # true 表示渲染前自动补齐缺失图片
   base_url = "http://127.0.0.1:8188"
   model_name = "z_image_turbo"
   width = 832
   height = 1216
   seed = 1024
   steps = 20
   cfg = 3.5
   ```
   - 若仅想按需触发，请将 `enable_auto_generate` 设为 `false`，并在 CLI 运行时使用 `--auto-images`。

## 3. 使用方式
### 3.1 CLI
```bash
python main.py --theme "城市微光" \
               --style "温暖治愈" \
               --image-style "电影级写实" \
               --scene-count 8 \
               --auto-images \
               --auto-confirm
```
- `--theme` 为必填项；可选参数包括 `--style`、`--image-style`、`--negative-prompt`、`--voice-id`、`--voice-speed`、`--scene-count`、`--aspect-ratio`、`--auto-images` 等。
- 不加 `--auto-confirm` 时会停在人工确认阶段；确认后可随时运行 `python main.py --project-id <id>` 继续渲染。

### 3.2 Web 前端
1. 运行 `uvicorn main:app --reload --port 8000`。
2. 浏览器访问 `http://localhost:8000/`。
3. 填写主题、语气、分镜数量、voice 等信息，点击“生成剧本与提示词”。
4. 通过表格逐个编辑镜头文本、图片提示词、负面提示词和图片路径。
5. 点击“保存镜头修改”后再点击“确认并合成”即可触发音频+视频生成。

## 4. 工作流
1. **生成剧本和提示词**：调用 Volcengine 模型产出旁白与图片提示词，保存到 SQLite。
2. **准备分镜图片**：
   - 自动：启用 `comfyui.enable_auto_generate` 或 CLI `--auto-images` 时，系统会请求 `http://127.0.0.1:8188/prompt`（默认 `z_image_turbo`）并生成文件 `assets/images/projects/project_<id>/scene_<n>.png`。
   - 手动：根据提示词在 ComfyUI/其它制图工具出图并覆盖上述路径。
3. **人工校验**：在 CLI 输出或前端表格中确认脚本、提示词、图片路径无误，可随时修改。
4. **合成阶段**：MiniMax TTS 生成旁白音频（`assets/audio/generated/project_<id>.mp3`），MoviePy 结合 ASR+AI 的时长、图片和 BGM 输出视频（`outputs/project_<id>.mp4`）。

## 5. 调用/运行日志
- **CLI**：建议 `python main.py ... 2>&1 | tee logs/cli_$(date +%Y%m%d_%H%M).log`（PowerShell 可使用 `Tee-Object`），便于记录完整调用链。
- **Web 服务**：`uvicorn main:app --log-level debug --log-config logging.yaml`，将运行异常写入 `logs/app.log`，把外部 API 请求（URL、耗时、trace_id）写入 `logs/call.log`。
- **ComfyUI 调用**：`comfyui_client` 的 `[ComfyUI]` 前缀输出可重定向到 `logs/comfyui.log`，如需请求体/响应体可在 client 中启用 `httpx` event hook。
- **排查建议**：保存 API trace_id、生成的音频/视频路径，出现异常时打包日志文件与核心配置（去除密钥）方便复现。

## 6. 常见问题
| 问题 | 处理方式 |
| ---- | -------- |
| `ModuleNotFoundError: fastapi` | 未安装依赖，执行 `pip install fastapi uvicorn`。 |
| `Missing storyboard images` | 指定路径下缺少 `scene_<n>.png`，按提示词生成后放回 `assets/images/projects/project_<id>/`。 |
| API 调用失败/超时 | 检查 `config.toml` 中 URL、API Key、模型参数是否正确，必要时更换网络环境。 |
| TTS 输出为空 | 确认 MiniMax 账户可用，`voice_setting` 与 `audio_setting` 符合官方要求。 |

## 7. 后续扩展建议
- 在 SQLite 中保留项目历史版本，支持回溯。
- 增加模板管理（不同主题/风格预设）。
- 引入批量渲染队列与状态监控面板。
